import React from 'react';
import { Typography, CssBaseline, AppBar, Toolbar, Button, Person } from '@material-ui/core';
import useStyles from '../styles';

const RightBar = () => {
    const classes = useStyles();
    return (
        <>
            <CssBaseline />

        </>
    );
}
export default RightBar;